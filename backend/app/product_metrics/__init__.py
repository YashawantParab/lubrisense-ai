"""Product / North-Star metrics (Phase 22). Every `Metric` declares a `MetricProvenance`
(`app.domain.enums.MetricProvenance`) — `MEASURED_PLATFORM_METRIC`, `DEMO_ESTIMATE`, or
`CONFIGURED_TARGET` — and these are never mixed into one number (CLAUDE.md "Customer /
Business Thinking"). See docs/PRODUCT_METRICS.md for the exact North Star definition and
every supporting metric's numerator/denominator.
"""
