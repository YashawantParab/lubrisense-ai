/** @deprecated import `toneForStatus` from `@/lib/terminology` directly — kept as a
 * re-export so existing pages don't need a mechanical import-path change this sprint. */
export { toneForStatus } from "@/lib/terminology";
